+ Error handling for stacks and random items. 
+ Preview button allows starting timers, stopping on end preview
+ Timers show progress for inline buttons when last item added or only item playing - maybe have them show in the sfx panel as well to show there is an active t imer?
+ UI optimisations for modal random group 
+ Disallow editing when button is playing. 

+ Sync has to do with Kenku keeping stale data. Fixed autocomplete. Syncing requires Kenku restart 
